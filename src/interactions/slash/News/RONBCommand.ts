import { ApplicationCommandData } from "discord.js";

const commandConfig = {
    name: "ronb",
    description: "Get last fetched post from Routine of Nepal banda"
} as ApplicationCommandData;

export default commandConfig;
